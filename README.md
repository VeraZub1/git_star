This repo has been created and called git_star
==============================================
chapter one. Git command.
------------------------
  git add file 
  git add *
  git push
  git clone 
  git commit -a
  git commit --ammend
  git pull 
  git pull -r
  git log 
  git checkout master
  
